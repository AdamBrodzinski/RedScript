# Alias @ with this.
@someProp = foo

# valid_q = true
   valid? = true

opts =|| {thing: false}

# has a do after if
if foo == false do
  # do stuff
end

# no do on end
if isValid? do
    return true
  else
    return false
end

$('#someID').on('click', (foo, bar) =>
  # do stuff
end);

Foo = React.createClass({
  render() {
    return (
      <div className='Foo'>
        Hello World!
      </div>
    );
  },
})
