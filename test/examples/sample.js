// Alias @ with this.
@someProp = foo

// valid_q = true
   valid? = true

const opts =|| {thing: false}

// has a do after if
if (foo == false do) {
  // do stuff
}

// no do on end
if (isValid? do) {
    return true
  } else {
    return false
}

$('#someID').on('click', (foo, bar) =>
  // do stuff
});

const Foo = React.createClass({
  render() {
    return (
      <div const className ='Foo'>
        Hello World!
      </div>
    );
  },
})
